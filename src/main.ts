import * as angular from 'angular';

let app = angular.module('awesomeApp', []);

app.controller('awesomeAppController', function($scope) {
        $scope.message = "Hello World!";
} );

// ...and so it begins... 
